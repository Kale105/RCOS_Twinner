# Data

Keep dataset files out of Git.

Suggested local layout:

```text
data/
  raw/
    pointclouds/
    images/
    calibration/
    labels/
  processed/
  external/
```

Use small synthetic fixtures in `tests/fixtures/` when test data is needed.

